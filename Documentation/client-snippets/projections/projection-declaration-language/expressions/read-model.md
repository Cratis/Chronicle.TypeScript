```typescript
class PdlExpressionsUserReadModel {
    name = '';               // Requires string
    loginCount = 0;           // Requires number
    isActive = false;         // Requires boolean
    createdAt = new Date();   // Requires timestamp
}
```
