```text
TypeScript does not support this workflow yet.
Reactors are created without constructor arguments and receive only the event and its context, so they cannot inject a store to read state; return events to append them.
```
