{
    "name": "chronicle-typescript-workspace",
    "private": true,
    "packageManager": "yarn@4.17.1",
    "workspaces": [
        "Source",
        "Samples/Console"
    ],
    "scripts": {
        "compile": "yarn workspaces foreach -A -t run compile",
        "build": "yarn workspaces foreach -A -t run build",
        "clean": "yarn workspaces foreach -A -t run clean",
        "ci": "yarn clean && yarn build && yarn workspace @cratis/chronicle test:standard-types && yarn workspace @cratis/chronicle test:native && yarn workspace @cratis/chronicle test",
        "publish-version": "node ./publish-version.js"
    },
    "dependencies": {
        "@cratis/chronicle.contracts": "19.4.0",
        "glob": "^13.0.6"
    }
}
